{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && vercel --token $VERCEL_TOKEN --project $VERCEL_PROJECT_ID --output dist",
    "test": "npm run build && npm run test"
  },
  "dependencies": {
    // ... existing dependencies
  },
  "devDependencies": {
    // ... existing devDependencies
  }
}